DOCKHAND_API_TOKEN=dh_replace_me
REVEILLE_PORT=8080
REVEILLE_TAG=latest

# Used by compose.yml outside GitLab CI. In GitLab CI, CI_REGISTRY_IMAGE is set
# automatically and should point at the project container registry.
CI_REGISTRY_IMAGE=registry.gitlab.com/your-group/reveille
