.workflow/
.env
.env.*
!.env.example
!internal/hosts

# Local runtime compose file; keep compose.example.yml tracked.
compose.yml

# Host configs may contain private hostnames/container names.
targets/

# Root-level binary from `go build`; does not ignore cmd/reveille/.
/reveille

#go build cache
.cache